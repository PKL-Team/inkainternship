<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <Button
        android:id="@+id/btnTanpaLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tanpa Login"
        android:layout_centerInParent="true"
        android:layout_above="@id/btnDenganLogin"/>
    <Button
        android:id="@+id/btnDenganLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Dengan Login"
        android:layout_centerInParent="true"/>

    <Button
        android:id="@+id/btnLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_above="@id/btnTanpaLogin"
        android:text="Coba Login" />

    <TextView
        android:id="@+id/scanFile"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_above="@id/btnLogin"
        android:text="File 1"
        android:gravity="center_horizontal"
        android:textColor="#000"
        android:padding="10dp"/>

</RelativeLayout>